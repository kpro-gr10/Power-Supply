var Entity = Backbone.Model.extend({
  defaults: {
    // The positions of the entity on the map
    "x": 0,
    "y": 0,

    "spriteString": "house_test.png"
  },

  // Abstract function to be implemented in child objects.
  // dt is the time since the previous update
  this.update = function(dt) {
  };

});
